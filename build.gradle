apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'org.javassist', name: 'javassist', version: '3.+'
    testCompile group: 'junit', name: 'junit', version: '4.+'
}
